export interface PersonModel {
    id: number;
    name: string;
    lastName: string;
    user: any; // Puedes crear una interfaz para UserData si lo necesitas
    company: any; // Puedes crear una interfaz para Company si lo necesitas
  }